(function(){
    function validaSeExiste(componente){ //string da classe aqui ppfv
        variavel = document.querySelectorAll(componente);
        if(variavel){
            return true
        }
        else{
            return false
        }
    }

    if(validaSeExiste('#form-kickoff')){
        
    }

    const DemandaAnalista = {
        Analista: String,
        demanda: {},
        
 };

})()